import { MagicDirective } from './magic.directive';

describe('MagicDirective', () => {
  it('should create an instance', () => {
    const directive = new MagicDirective();
    expect(directive).toBeTruthy();
  });
});
