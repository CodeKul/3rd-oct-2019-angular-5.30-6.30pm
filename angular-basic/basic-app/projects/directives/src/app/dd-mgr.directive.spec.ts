import { DdMgrDirective } from './dd-mgr.directive';

describe('DdMgrDirective', () => {
  it('should create an instance', () => {
    const directive = new DdMgrDirective();
    expect(directive).toBeTruthy();
  });
});
